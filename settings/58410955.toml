# Title Name: Banjo-Tooie
# Title ID: 58410955

[GPU]
query_occlusion_sample_lower_threshold = 0 # Stops the lights rendering through the walls
query_occlusion_sample_upper_threshold = 0 # Stops the lights rendering through the walls